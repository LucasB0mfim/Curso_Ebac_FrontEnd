// Multiplicar números:
function multiplicador(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

// Saldação:
function saldacao(nome: string): string {
    return `Olá ${nome}, tudo bem?`
}

console.log(multiplicador(9.5, 2));
console.log(saldacao('lucas'));
